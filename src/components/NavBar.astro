---
const navItems = [
  { name: "Inicio", href: "/" },
  { name: "Compatibilidad", href: "/compatibility" },
  { name: "Carta Natal", href: "/natal-chart" }
];
---

<nav class="bg-black bg-opacity-80 border-b border-purple-800 sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between">
    <a href="/" class="text-2xl font-bold text-purple-300 tracking-wide">🔮 AstroAura</a>
    <ul class="hidden md:flex space-x-6 text-sm text-purple-200">
      {navItems.map(item => (
        <li><a href={item.href} class="hover:text-white transition">{item.name}</a></li>
      ))}
    </ul>
    <button class="md:hidden text-purple-400">☰</button>
  </div>
</nav>
